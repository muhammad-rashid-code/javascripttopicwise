// //In JavaScript, you can change elements of a date and time using the built-in Date object's methods. Here are some examples of how to change different elements of a Date object:
// //Create a new Date object with the current date and time
// const date =new Date();

// //Get the Year and set it to a new value
// const year=date.getFullYear();
// date.setFullYear(year+1);

// //get the month and set into a new value
// const month=date.getMonth();
// date.setMonth(month+1);

// //get the day and set into a new value
// const day=date.getDate();
// date.setDate(day+1);

// const hour=date.getHours();
// date.setHours(hour+1);

// const minute=date.getMinutes();
// date.setMinutes(minute);

// const seconds=date.setSeconds();
// date.setSeconds(seconds+1);

// console.log(date);

// // Create a new Date object with the current date and time
// const date = new Date();

// // Get the year and set it to a new value
// const year = date.getFullYear();
// date.setFullYear(year + 1);

// // Get the month and set it to a new value
// const month = date.getMonth();
// date.setMonth(month + 1);

// // Get the day and set it to a new value
// const day = date.getDate();
// date.setDate(day + 1);

// // Get the hour and set it to a new value
// const hour = date.getHours();
// date.setHours(hour + 1);

// // Get the minute and set it to a new value
// const minute = date.getMinutes();
// date.setMinutes(minute + 1);

// // Get the second and set it to a new value
// const second = date.getSeconds();
// date.setSeconds(second + 1);

// // Output the updated date and time
// console.log(date); // Output: Updated date and time based on the changes made above

// //In the example above, a new Date object is created with the current date and time. Then, the Date object's methods are used to get different elements of the date and time (year, month, day, hour, minute, second), and set them to new values by adding 1 to their current values. Finally, the updated Date object is outputted to the console.

// //Note that the Date object's methods that change individual elements (such as setFullYear(), setMonth(), setDate(), setHours(), setMinutes(), setSeconds()) modify the Date object in place, rather than returning a new Date object.